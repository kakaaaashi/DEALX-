<%- include('layout', { body: `
  <h2>All Items</h2>
  <div class="grid">
    ${products.length ? products.map(p => `
      <article class="card">
        <a href="/item/${p.id}"><img src="${p.image_url? p.image_url : 'https://images.unsplash.com/photo-1518779578993-ec3579fee39f?auto=format&fit=crop&w=1200&q=80'}" alt="${p.name}"></a>
        <div class="card-body">
          <h3><a href="/item/${p.id}">${p.name}</a></h3>
          <p class="muted">${(p.description||'').substring(0,140)}${(p.description||'').length>140?'...':''}</p>
          <p class="price">₹${p.price}</p>
        </div>
      </article>
    `).join('') : `<div class="empty">No items available.</div>`}
  </div>
` }) %>